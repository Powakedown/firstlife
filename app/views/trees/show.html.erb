<ul class="tree">
  <li>
    <div class="leaf">
     <h1><%= @tree.name %></h1>
    </div>
    <ul>
      <li>
      <%= @skills.each do |skill| %>
        <div class="leaf"><h2><%= @skills.parent.name %></h2></div>
        <ul>
          <%= @skills.each do |skill| %>
          <li>
            <div class="leaf"><%= @skills.children.name %></div>
          </li>
          <% end %>
        </ul>
      <% end  %>
      </li>

      <li>
        <div class="leaf">HTML</div>
      </li>
      <li>
        <div class="leaf">CSS</div>
      </li>
      <li>
        <div class="leaf">SVG</div>
      </li>
    </ul>
  </li>
</ul>

<% @skills.each do |skill| %>
  <h2><%= skill.name %></h2>
  <% if skill.description %>
    <!-- <h2><%= skill.description %></h2> -->
  <% end %>
<% end %>
