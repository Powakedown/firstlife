$('#skill-<%= @skill.id %>').html('<%= j render "skills/skill", skill: @skill %>');

<% @skill.children.each do |skill| %>
  <% if skill.level %>
    $('#skill-<%= skill.id %>').html('<%= j render "skills/skill", skill: skill %>');
  <% end %>
<% end %>

$('#title').removeClass("expandOpen");
window.requestAnimationFrame(function(time) {
window.requestAnimationFrame(function(time) {
    $('#title').addClass("expandOpen");
  });
  });

$('#title .skill-count').text('<%= current_user.skills.where(tree: @skill.tree, level: nil).count %>');

